//The Javascript is connected with the alert...
alert('JS is Connected!');

//With JS we can access everythings what we can need so...
    console.log(document.head);
    console.log(document.body);
    console.log(document);
    console.log(document.doctype);
    console.log(document.all);
    console.log(document.all[8]);
    console.dir(document);

//What can do the javascript...
    var text = document.querySelector('.textAreaOne');
    text.textContent = 'Asad Anik';

//Another way...
    var list = document.querySelector('li');
    
    for(var i = 0; i < li.length; i++){
        li[i].setAttribute('style', 'background : gray');
    }
    
//For the form area control with the javascript...
    var submit = document.querySelector('input[type="submit"]');
    submit.value = 'SEND';
    

